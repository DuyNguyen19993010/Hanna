const SearchBarWrapper = ({children}) => {
    
    return (
        
        <div id = "util-bar-wrapper">
        
            {children}
        
        </div>
    
    )

}

export default SearchBarWrapper;

